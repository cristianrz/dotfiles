#!/bin/env bash

cd "$(dirname "$0")" || exit
cd project.d || exit

noun=$(shuf -n 1 nouns.txt)
adjective=$(shuf -n 1 adjectives.txt)
echo "$adjective-$noun"
adjective=${adjective^}
noun=${noun^}
echo "$adjective $noun"
