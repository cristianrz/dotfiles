#!/bin/env sh
#
# Copyright 2019 Cristian Ariza
#
# See LICENSE file for license details.
# ~/.bashrc: executed by bash(1) for non-login shells.
#
# Exported variables

test -d "$HOME"/go/bin && export PATH="$HOME/go/bin:${PATH}" && export GOBIN="$HOME/go/bin"
test -d "$HOME"/.local/bin && export PATH="$HOME"/.local/bin:"$PATH"
test -d ~/go && export GOPATH="$HOME/go"
test -d ~/scripts && export PATH="$HOME/scripts:${PATH}"
export TERM="screen-256color"

# shellcheck disable=SC2063
# shellcheck disable=SC1117
PS1="\[\e[97;44m\] \u \[\e[37;100m\] \W \$ \[\e[m\] "
export PS1
# Infinite history.
HISTSIZE='' HISTFILESIZE=
