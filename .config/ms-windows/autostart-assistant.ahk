if WinExist("Everything")
    WinClose ; use the window found above
	
if WinExist("Logitech")
    WinClose ; use the window found above
	
if WinExist("Skype")
    WinClose ; use the window found above
	
if WinExist("Firefox")
    WinClose ; use the window found above
	
if WinExist("autostart")
    WinClose ; use the window found above